# UPSTREAM SOURCE OF TRUTH
This repo is a published downstream mirror of a deck. The canonical upstream auto-loads via the operator's local ~/.claude registry; deliberately not committed here because this repo is public.
